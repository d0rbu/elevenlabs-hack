import { text } from "$lib/flags";

const AGENT_NAME = "Gwen";
const NAME = "AI dating";
const TAGLINE = "Dating reimagined";

export const agentName = text(AGENT_NAME);
export const name = text(NAME);
export const tagline = text(TAGLINE);
export const IMAGE_CAPTIONING_MODEL = "fal-ai/florence-2-large/more-detailed-caption";
